#include <iostream>
#include <sys/types.h>
#include <sys/socket.h>
#include <unistd.h>

using namespace std;

void error(char *msg)
{
  perror(msg);
  exit(0);
}

int main()
{
  
  
  
  
  
  return 0;
}
